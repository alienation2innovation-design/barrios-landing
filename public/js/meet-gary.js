(() => {
  // Meet Gary section behaviour.
  //
  // The "Start a consultation request" control in this section is an ordinary
  // NEXUS opener: the shared [data-nexus-open] handler in index.html opens the
  // panel and keeps aria-expanded in sync, so nothing else is wired here.
  //
  // For the NEXUS public pilot this file intentionally does not alter the
  // panel copy: the assistant collects consultation requests and makes no
  // claim about arranging meetings.
})();
